Day 1/2 — 18% complete. Covered: SELECT, DISTINCT, COUNT, aliases (AS), Views, WHERE, AND/OR, IN, BETWEEN, LIKE, ORDER BY, GROUP BY, HAVING, ROUND, integer division.
Day 27 (SQL) - ~28% complete. Covered: CASE in SELECT/WHERE/COUNT-SUM-AVD, correlated vs independent subqueries, capital cities EXISTS exercise.
Day 28 (SQL) - ~34% complete. Covered subqueries in WHERE/SELECT/FROM (scalar, nested, correlated), CTEs with WITH (single and chained), window functions intro - OVER(), RANK(), core mechanic of per-row calculations without collapsing rows.
Day 29 (SQL) - ~38% complete. Covered: ROW_NUMBER, RANK vs ROW_NUMBER (ties), LAG/LEAD, PARTITION BY, alias-timing rule in window functions.
